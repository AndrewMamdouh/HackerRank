import { utopianTree } from '@ProblemSolving/Algorithms/Implementation';

describe('Problem Solving » Algorithms » Implementation » Utopian Tree', () => {
    it('should return how tall will the tree be', () => {
        expect(utopianTree(4)).toBe(7);
    });
});
